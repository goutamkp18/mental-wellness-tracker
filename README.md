
to run this :
got to VS code and open a folder, then go to termminal and use the following commands: 
> git init
> git clone https://github.com/goutamkp18/inner-bloom-hub.git\n
then go the project folder:
> sudo apt update && sudo apt upgrade -y
> sudo apt install nodejs npm -y
> node -v
> npm -v
> npm install
> npm run dev
